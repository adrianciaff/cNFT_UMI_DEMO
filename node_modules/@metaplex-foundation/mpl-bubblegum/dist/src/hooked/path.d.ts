import { PublicKey } from '@metaplex-foundation/umi';
import { Serializer } from '@metaplex-foundation/umi/serializers';
export type Path = {
    proof: PublicKey[];
    leaf: PublicKey;
    index: number;
};
export type PathArgs = Path;
export declare function getPathSerializer(maxDepth: number): Serializer<PathArgs, Path>;
